import swaggerDocument from "./swagger/index";

/**
 * Swagger specification
 * We now use a more modular approach with separate files for each entity
 */
const swaggerSpec = swaggerDocument;

export default swaggerSpec;
