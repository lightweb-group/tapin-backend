# Environment variables
NODE_ENV=development
PORT=9000

# Add any other environment variables your application needs here 

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# TODO: Update the following URL with your actual database credentials
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://mahato:mahato@localhost:5432/tap-in?schema=public"

# Security-related environment variables
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:9000

# JWT Secret for authentication (minimum 32 characters)
JWT_SECRET=your_jwt_secret_key_here_min_32_chars_long

# CORS settings
CORS_ENABLED=true

# Rate limiting settings
RATE_LIMIT_STANDARD_MAX=100
RATE_LIMIT_LOGIN_MAX=5
RATE_LIMIT_API_MAX=50

# Request timeout in milliseconds (default: 30 seconds)
REQUEST_TIMEOUT=30000

# Content security policy settings
CSP_ENABLED=true

# CSRF protection
CSRF_ENABLED=true